/* Aritmétricos 
    + Adição / Concatenação
    ** Potenciação
    % Resto da divisão
    Incremento = ++ (soma de 1 em 1)
    Incremento = += 2 (soma de 1 ou pra mais)
    Decremento = -- (subtrai de 1 em 1)
    Decremento = -= (subtrai de 1 ou mais)
    Passar para Inteiro = parseInt('5.2')
    Passar para Float = parseFloat('5.2')
*/

let contador = 0;
contador += 2;
contador += 2;
contador -= 2;
console.log(contador)